import 'package:flutter/material.dart';

class SelectTherapistScreen extends StatelessWidget {
  const SelectTherapistScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Select a therapist'),

      ),
    );
  }
}
